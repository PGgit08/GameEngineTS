/**
 * Contains the names of Buffers in Geometries.
 */
export class BufferConfig {
    public static readonly NAMES = {
        POSITION_BUFFER_NAME: "POSITION_BUFFER",
        TEXTURE_BUFFER_NAME: "TEXTURE_BUFFER"
    }
}
